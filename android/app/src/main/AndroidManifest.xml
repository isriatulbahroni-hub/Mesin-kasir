<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Wajib untuk semua request ke Supabase (auth, database, storage, realtime). -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Kamera untuk scan barcode (mobile_scanner) dan foto produk (image_picker). -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- Bluetooth klasik untuk printer struk thermal 58mm/80mm
         (print_bluetooth_thermal). Android 12+ (API 31+) pakai model
         permission baru (BLUETOOTH_SCAN/CONNECT); di bawahnya pakai
         BLUETOOTH/BLUETOOTH_ADMIN + lokasi (wajib dari OS untuk scan BT
         classic sebelum Android 12, bukan karena app ini butuh lokasi). -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>

    <application
        android:label="Kasir Pro"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ML Kit (dipakai mobile_scanner) auto-install modul barcode scanning
             lewat Google Play Services SAAT APLIKASI DI-INSTALL, bukan lazy-load
             pas pertama kali buka scanner. Ini rekomendasi resmi Google:
             https://developers.google.com/ml-kit/vision/barcode-scanning/android#4-optional-configure-app-to-automatically-download-the-ml-model-to-device-after-your-app-is-installed-from-the-play-store
             Ditambahkan sebagai langkah mitigasi bug "kamera gagal dibuka di
             release build" (genericError/getClass on null) yang juga
             dilaporkan user lain di issue tracker mobile_scanner - kemungkinan
             modul ML Kit belum siap saat kamera pertama kali dibuka. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
